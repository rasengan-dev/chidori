/**
 * Define static paths for a page
 * @param items - List of params
 * @returns
 */
export declare function defineStaticPaths<T extends Record<string, string>>(items: T[]): {
    paths: {
        params: T;
    }[];
};
