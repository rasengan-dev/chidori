export declare const extensions: string[];
/**
 * Asynchronously loads a module from a file path only in the server-side environment.
 *
 * @param path - The path to the module.
 * @returns The loaded module.
 */
export declare const loadModuleSSR: (path: string) => Promise<any>;
/**
 * Finds the module path with the right extension.
 *
 * @param path - The path to the module.
 * @returns The module path and extension.
 */
export declare const findModulePath: (path: string) => Promise<{
    path: string;
    extension: string;
}>;
export declare const getDirname: (url: string) => Promise<string>;
