import type { AppConfig, AppConfigFunction, AppConfigFunctionAsync } from '../type.js';
/**
 * Function to define the config for the app
 * It will be used by vite.config.ts and other files in other to configure the app
 * @param {AppConfig | AppConfigFunction | AppConfigFunctionAsync} loadedConfig
 */
export declare const defineConfig: (loadedConfig: AppConfig | AppConfigFunction | AppConfigFunctionAsync) => Promise<() => Promise<AppConfig>>;
