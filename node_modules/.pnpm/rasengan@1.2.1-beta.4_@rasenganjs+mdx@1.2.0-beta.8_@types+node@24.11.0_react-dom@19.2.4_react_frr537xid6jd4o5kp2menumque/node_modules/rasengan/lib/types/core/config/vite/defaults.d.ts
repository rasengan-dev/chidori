import type { UserConfig } from 'vite';
import type { AppConfig } from '../type.js';
export declare const createDefaultViteConfig: (rootPath: string, __dirname: string, mode: string, config: AppConfig) => UserConfig;
