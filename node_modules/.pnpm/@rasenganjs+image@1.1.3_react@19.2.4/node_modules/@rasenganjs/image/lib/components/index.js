import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
// Lazy load the actual image component
const LazyLoadedImage = React.lazy(() => import('./image.js'));
// Fallback component to show while the image is loading
export const LoadingFallback = ({ width, height }) => (_jsx("div", { style: {
        width,
        height,
        backgroundColor: '#e5e5e5',
    } }));
export const LazyImage = ({ src, alt, loading = 'lazy', mode = 'wave', ...props }) => {
    return (_jsx(React.Suspense, { fallback: _jsx(LoadingFallback, { width: props.width || 200, height: props.height || 200 }), children: _jsx(LazyLoadedImage, { src: typeof src === 'string' ? src : src.uri, alt: alt, loading: loading, mode: mode, ...props }) }));
};
//# sourceMappingURL=index.js.map