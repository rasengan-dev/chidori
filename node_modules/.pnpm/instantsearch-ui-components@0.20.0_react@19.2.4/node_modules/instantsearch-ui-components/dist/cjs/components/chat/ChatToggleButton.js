'use strict';

var _object_spread = require('@swc/helpers/_/_object_spread');
var _object_without_properties = require('@swc/helpers/_/_object_without_properties');
var cx = require('../../lib/cx.js');
var Button = require('../Button.js');
var icons = require('./icons.js');

function createChatToggleButtonComponent(param) {
    var createElement = param.createElement;
    var Button$1 = Button.createButtonComponent({
        createElement: createElement
    });
    return function ChatToggleButton(userProps) {
        var open = userProps.open, onClick = userProps.onClick, ToggleIcon = userProps.toggleIconComponent, _userProps_classNames = userProps.classNames, classNames = _userProps_classNames === void 0 ? {} : _userProps_classNames, className = userProps.className, props = _object_without_properties._(userProps, [
            "open",
            "onClick",
            "toggleIconComponent",
            "classNames",
            "className"
        ]);
        var defaultIcon = open ? /*#__PURE__*/ createElement(icons.ChevronUpIcon, {
            createElement: createElement
        }) : /*#__PURE__*/ createElement(icons.SparklesIcon, {
            createElement: createElement
        });
        return /*#__PURE__*/ createElement(Button$1, _object_spread._({
            variant: "primary",
            size: "md",
            iconOnly: true,
            className: cx.cx('ais-ChatToggleButton', open && 'ais-ChatToggleButton--open', classNames.root, className),
            onClick: onClick
        }, props), ToggleIcon ? /*#__PURE__*/ createElement(ToggleIcon, {
            isOpen: open
        }) : defaultIcon);
    };
}

exports.createChatToggleButtonComponent = createChatToggleButtonComponent;
