'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _object_spread = require('@swc/helpers/_/_object_spread');
var _object_spread_props = require('@swc/helpers/_/_object_spread_props');
var connectConfigure = require('../../connectors/configure/connectConfigure.js');
var noop = require('../../lib/utils/noop.js');

var configure = function configure(widgetParams) {
    // This is a renderless widget that falls back to the connector's
    // noop render and unmount functions.
    var makeWidget = connectConfigure.default(noop.noop);
    return _object_spread_props._(_object_spread._({}, makeWidget({
        searchParameters: widgetParams
    })), {
        $$widgetType: 'ais.configure'
    });
};

exports.default = configure;
