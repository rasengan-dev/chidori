'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var instantsearchUiComponents = require('instantsearch-ui-components');
var preact = require('preact');

function Selector(param) {
    var currentValue = param.currentValue, options = param.options, cssClasses = param.cssClasses, setValue = param.setValue, ariaLabel = param.ariaLabel;
    return /*#__PURE__*/ preact.h("select", {
        className: instantsearchUiComponents.cx(cssClasses.select),
        onChange: function onChange(event) {
            return setValue(event.target.value);
        },
        value: "".concat(currentValue),
        "aria-label": ariaLabel
    }, options.map(function(option) {
        return /*#__PURE__*/ preact.h("option", {
            className: instantsearchUiComponents.cx(cssClasses.option),
            key: option.label + option.value,
            value: "".concat(option.value)
        }, option.label);
    }));
}

exports.default = Selector;
