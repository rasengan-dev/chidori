import { type Plugin } from 'vite';
export declare const Adapters: {
    readonly VERCEL: "vercel";
    readonly DEFAULT: "";
};
export type Adapter = (typeof Adapters)[keyof typeof Adapters];
export interface AdapterOptions {
}
export interface AdapterConfig {
    name: Adapter;
    prepare: () => Promise<void>;
}
type RasenganPluginOptions = {
    adapter?: AdapterConfig;
};
/**
 * This plugin is responsible for building the app.
 * @param param0
 * @returns
 */
export declare function rasengan({ adapter, }?: RasenganPluginOptions): Plugin;
export declare const plugins: Plugin[];
export {};
