const defaultBuildDirectory = "./dist";
export const resolveBuildOptions = ({ buildDirectory = defaultBuildDirectory, clientPathDirectory = 'client', serverPathDirectory = 'server', }) => {
    return {
        buildDirectory: buildDirectory,
        manifestPathDirectory: '.vite',
        assetPathDirectory: 'assets',
        clientPathDirectory,
        serverPathDirectory,
        entryServerPath: 'entry.server.js',
    };
};
